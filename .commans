# Выполняет ребейз с самого первого коммита
git rebase -i --root
# Получает версию текущего крейта
cargo pkgid | cut -d "#" -f2
# Выполняет слияние git с полученной версией
git merge --no-ff $version
